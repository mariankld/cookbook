# Family Cookbook Web

Minimal Next.js recipe website powered by Supabase.

## Run locally

1. Copy `.env.example` to `.env.local`
2. Fill in `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`
3. Start dev server:

```bash
npm run dev
```

Open http://localhost:3000.
